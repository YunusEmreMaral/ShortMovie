@model List<Movie>
@{
	int i = 0; 
}
<div class="sb-recentpost sb-it">
	<h4 class="sb-title">Son Filmler</h4>

	@foreach(var item in Model)
	{
		i++;
		<div class="recent-item">
			<span>@i</span><h6><a href ="/MovieDetail/Movie/@item.MovieID" style="font-size:2rem" >@item.MovieName</a></h6>
		</div>
	}
	
	<a >
</div>